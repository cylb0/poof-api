export enum Assets {
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
}

export enum SceneElementType {
  IMAGE = Assets.IMAGE,
  VIDEO = Assets.VIDEO,
  AUDIO = Assets.AUDIO,
  TEXT = 'text',
}
